import React from 'react';
import './CreatedEvents.scss';

class CreatedEvents extends React.Component {
  render() {
    return (
        <div> see all created events</div>
    );
  }
}

export default CreatedEvents;
